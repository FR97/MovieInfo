<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <androidx.cardview.widget.CardView
        style="@style/CardViewStyle"
        android:layout_margin="@dimen/default_margin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- YouTube trailer thumbnail -->
            <ImageView
                android:id="@+id/imageVideoThumbnail"
                android:layout_width="@dimen/width_video_thumb"
                android:layout_height="@dimen/height_video_thumb"
                android:layout_gravity="center"
                android:contentDescription="@string/cd_video_thumbnail" />

            <!-- Play button image -->
            <ImageView
                android:id="@+id/imageVideoPlay"
                android:layout_width="@dimen/size_play_image"
                android:layout_height="@dimen/size_play_image"
                android:layout_gravity="center_horizontal|center_vertical"
                android:background="@drawable/ic_play_circle_outline_secondary_24dp"
                android:contentDescription="@string/cd_play_image" />
        </FrameLayout>
    </androidx.cardview.widget.CardView>
</layout>
